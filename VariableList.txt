name - endpoint
datasource - prometheus
query - label_values(http_requests_total, endpoint)



name - status_class
datasource - prometheus
query - label_values(http_requests_total, status_class)


name - method
datasource - prometheus
query - label_values(http_requests_total, method)


promql - 

sum(rate(http_requests_total{endpoint="$endpoint"}[1m]))
sum(rate(http_requests_total{status_class="5xx"}[1m]))
sum(rate(http_requests_total{status_class="2xx"}[1m]))

success vs failour
sum(rate(http_requests_total{status_class=~"2xx|5xx"}[1m]))
